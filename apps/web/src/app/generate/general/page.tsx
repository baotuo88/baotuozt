import { GenerationPage } from '@/components/generation/GenerationPage';

export default function GeneralPage() {
  return (
    <GenerationPage
      mode="general"
      title="🎨 通用图片生成"
      description="自由创作，将你的想象变为现实"
    />
  );
}
